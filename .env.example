# Copy to `.env.local` for local builds. Set in Cloudflare Pages project env for production.

# Deploy `workers/chatbot-api` first, then set your Worker URL (no trailing slash).
# Example: https://travel-with-moiz-chatbot-api.<your-subdomain>.workers.dev
NEXT_PUBLIC_CHAT_API_URL=
